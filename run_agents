#!/bin/bash

PROJECT_ROOT="$(cd "$(dirname "$0")" && pwd)"
BUILD_AGENT_DIR="$PROJECT_ROOT/build/agents"
LOG_DIR="$PROJECT_ROOT/log"
SESSION="exchange_agents"

mkdir -p "$LOG_DIR"

# Check if session already exists
if tmux list-sessions -F '#S' 2>/dev/null | grep -q -x "$SESSION"; then
    echo "Session '$SESSION' is already running in the background."
    exit 0
fi

# If binaries not found or not compiled, run make
if [ ! -d "$BUILD_AGENT_DIR" ] || [ -z "$(find "$BUILD_AGENT_DIR" -maxdepth 1 -type f -executable 2>/dev/null)" ]; then
    echo "Agent binaries not found or not compiled. Running 'make'..."
    make -C "$PROJECT_ROOT"
    if [ $? -ne 0 ]; then
        echo "Error: 'make' failed."
        exit 1
    fi
fi

# Find all executable agents dynamically
AGENTS=($(find "$BUILD_AGENT_DIR" -maxdepth 1 -type f -executable | sort))

if [ ${#AGENTS[@]} -eq 0 ]; then
    echo "Error: No executable agents found in $BUILD_AGENT_DIR even after running make."
    exit 1
fi

echo "Starting $SESSION agents in the background (detached tmux session)..."

# Start a new detached session with the first agent
first_exe="${AGENTS[0]}"
first_name=$(basename "$first_exe")
log="$LOG_DIR/${first_name}.log"

tmux new-session -d -s "$SESSION" -n "$first_name" "bash -c 'while true; do echo \"--- Starting $first_name ---\"; $first_exe 2>&1 | tee -a $log; echo \"--- $first_name stopped/crashed, restarting in 2s ---\"; sleep 2; done'"

# Configure status bar for help
tmux set-option -t "$SESSION" status-bg black
tmux set-option -t "$SESSION" status-fg white

# Bind Ctrl+B + Arrows to switch windows easily
tmux bind-key -r Left select-window -t :-
tmux bind-key -r Right select-window -t :+

# Create windows for the remaining agents
for ((i=1; i<${#AGENTS[@]}; i++)); do
    exe="${AGENTS[$i]}"
    name=$(basename "$exe")
    log="$LOG_DIR/${name}.log"
    
    # Run the agent in a loop for auto-restart
    tmux new-window -t "$SESSION" -n "$name" "bash -c 'while true; do echo \"--- Starting $name ---\"; $exe 2>&1 | tee -a $log; echo \"--- $name stopped/crashed, restarting in 2s ---\"; sleep 2; done'"
done

# Select the first window by default
tmux select-window -t "$SESSION":0

echo "Agents successfully started. To view them, run: tmux attach -t $SESSION"
